on 1:input:*: {
  if ($left($1,1) = /) { return }
  elseif (%input == ON) && ($1 == %input1) { msg $active %input [ $+ [ $1 ] ]  $2- | halt }
  elseif (%input == ON) && ($1 == %input2) { msg $active %input [ $+ [ $1 ] ]  $2- | halt }
  elseif (%input == ON) && ($1 == %input3) { msg $active %input [ $+ [ $1 ] ]  $2- | halt }
  elseif (%input == ON) && ($1 == %input4) { msg $active %input [ $+ [ $1 ] ]  $2- | halt }
  elseif (%input == ON) && ($1 == %input5) { msg $active %input [ $+ [ $1 ] ]  $2- | halt }
  elseif (%input == ON) && ($1 == %input6) { msg $active %input [ $+ [ $1 ] ]  $2- | halt }
  elseif (%input == ON) && ($1 == %input7) { msg $active %input [ $+ [ $1 ] ]  $2- | halt }
  elseif (%input == ON) && ($1 == %input8) { msg $active %input [ $+ [ $1 ] ]  $2- | halt }
  elseif (%input == ON) && ($1 == %input9) { msg $active %input [ $+ [ $1 ] ]  $2- | halt }
  elseif (%input == ON) && ($1 == %input10) { msg $active %input [ $+ [ $1 ] ]  $2- | halt }
  elseif (%input == ON) && ($1 == %input11) { msg $active %input [ $+ [ $1 ] ]  $2- | halt }
  elseif (%input == ON) && ($1 == %input12) { msg $active %input [ $+ [ $1 ] ]  $2- | halt }
  elseif (%input == ON) && ($1 == %input13) { msg $active %input [ $+ [ $1 ] ]  $2- | halt }
  elseif (%input == ON) && ($1 == %input14) { msg $active %input [ $+ [ $1 ] ]  $2- | halt }
  elseif (%input == ON) && ($1 == %input15) { msg $active %input [ $+ [ $1 ] ]  $2- | halt }
  elseif (%input == ON) && ($1 == %input16) { msg $active %input [ $+ [ $1 ] ]  $2- | halt }
  elseif (%input == ON) && ($1 == %input17) { msg $active %input [ $+ [ $1 ] ]  $2- | halt }
  elseif (%input == ON) && ($1 == %input18) { msg $active %input [ $+ [ $1 ] ]  $2- | halt }
  elseif (%input == ON) && ($1 == %input19) { msg $active %input [ $+ [ $1 ] ]  $2- | halt }
  elseif (%input == ON) && ($1 == %input20) { msg $active %input [ $+ [ $1 ] ]  $2- | halt }
  elseif (%input == ON) && ($1 == %input21) { msg $active %input [ $+ [ $1 ] ]  $2- | halt }
  elseif (%input == ON) && ($1 == %input22) { msg $active %input [ $+ [ $1 ] ]  $2- | halt }
  elseif (%input == ON) && ($1 == %input23) { msg $active %input [ $+ [ $1 ] ]  $2- | halt }
  elseif (%input == ON) && ($1 == %input24) { msg $active %input [ $+ [ $1 ] ]  $2- | halt }
  elseif (%input == ON) && ($1 == %input25) { msg $active %input [ $+ [ $1 ] ]  $2- | halt }
  elseif (%input == ON) && ($1 == %input26) { msg $active %input [ $+ [ $1 ] ]  $2- | halt }
  elseif (%input == ON) && ($1 == %input27) { msg $active %input [ $+ [ $1 ] ]  $2- | halt }
  elseif (%input == ON) && ($1 == %input28) { msg $active %input [ $+ [ $1 ] ]  $2- | halt }
  elseif (%input == ON) && ($1 == %input29) { msg $active %input [ $+ [ $1 ] ]  $2- | halt }
  elseif (%input == ON) && ($1 == %input30) { msg $active %input [ $+ [ $1 ] ]  $2- | halt }
  elseif (%input == ON) && ($1 == %input31) { msg $active %input [ $+ [ $1 ] ]  $2- | halt }
  elseif (%input == ON) && ($1 == %input32) { msg $active %input [ $+ [ $1 ] ]  $2- | halt }
  elseif (%input == ON) && ($1 == %input33) { msg $active %input [ $+ [ $1 ] ]  $2- | halt }
  elseif (%input == ON) && ($1 == %input34) { msg $active %input [ $+ [ $1 ] ]  $2- | halt }
  elseif (%input == ON) && ($1 == %input35) { msg $active %input [ $+ [ $1 ] ]  $2- | halt }
  elseif (%input == ON) && ($1 == %input36) { msg $active %input [ $+ [ $1 ] ]  $2- | halt }
  elseif (%input == ON) && ($1 == %input37) { msg $active %input [ $+ [ $1 ] ]  $2- | halt }
  elseif (%input == ON) && ($1 == %input38) { msg $active %input [ $+ [ $1 ] ]  $2- | halt }
  elseif (%input == ON) && ($1 == %input39) { msg $active %input [ $+ [ $1 ] ]  $2- | halt }
  elseif (%input == ON) && ($1 == %input40) { msg $active %input [ $+ [ $1 ] ]  $2- | halt }
  elseif (%input == ON) && ($1 == %input41) { msg $active %input [ $+ [ $1 ] ]  $2- | halt }
  elseif (%input == ON) && ($1 == %input42) { msg $active %input [ $+ [ $1 ] ]  $2- | halt }
  elseif (%input == ON) && ($1 == %input43) { msg $active %input [ $+ [ $1 ] ]  $2- | halt }
  elseif (%input == ON) && ($1 == %input44) { msg $active %input [ $+ [ $1 ] ]  $2- | halt }
  elseif (%input == ON) && ($1 == %input45) { msg $active %input [ $+ [ $1 ] ]  $2- | halt }
  elseif (%input == ON) && ($1 == %input46) { msg $active %input [ $+ [ $1 ] ]  $2- | halt }
  elseif (%input == ON) && ($1 == %input47) { msg $active %input [ $+ [ $1 ] ]  $2- | halt }
  elseif (%input == ON) && ($1 == %input48) { msg $active %input [ $+ [ $1 ] ]  $2- | halt }
  elseif (%input == ON) && ($1 == %input49) { msg $active %input [ $+ [ $1 ] ]  $2- | halt }
  elseif (%input == ON) && ($1 == %input50) { msg $active %input [ $+ [ $1 ] ]  $2- | halt }
  elseif (%fantastico == ON) {
    .set %setfont $1-
    .say $replace(%setfont,ch,%setfontch,sh,%setfontsh,a,%setfonta,b,%setfontb,c,%setfontc,d,%setfontd,e,%setfonte,f,%setfontf,g,%setfontg,h,%setfonth,i,%setfonti,j,%setfontj,k,%setfontk,l,%setfontl,m,%setfontm,n,%setfontn,o,%setfonto,p,%setfontp,q,%setfontq,r,%setfontr,s,%setfonts,t,%setfontt,u,%setfontu,v,%setfontv,w,%setfontw,x,%setfontx,y,%setfonty,z,%setfontz)                                     
    .halt
  }
  elseif (%inputset == ON) {
    if (%renautocolor == 1) {
      .say %ghablekalame_ $+  $+ %renco1 $+ , $+ %rencoba1 $+ %BBB $+ $1- $+ %BBB %badekalame_
      .halt
    }
    if (%renautocolor == 2) {
      set %§ 0
      set %rencolor %renco2-1
      :rep2
      inc %§
      if ($ [ $+ [ %§ ] ]) {
        set %rentxt $ [ $+ [ %§ ] ]
        set %renlen $len(%rentxt)
        set %rentxtt  $+ %rencolor $left($ [ $+ [ %§ ] ],1)
        if (%rencolor == %renco2-1) { set %rencolor %renco2-2 }
        elseif (%rencolor == %renco2-2) { set %rencolor %renco2-1 }
        set %ren 1
        set %renn 0
        :ren2
        inc %ren
        inc %renn
        if (%ren > %renlen) { set %ren-txt %ren-txt $+ %rentxtt | goto rep2 }
        if (%rencolor == %renco2-1) { set %rentxtt %rentxtt $+  $+ %renco2-1 $+ $remove($left(%rentxt,%ren),$left(%rentxt,%renn)) | set %rencolor %renco2-2 }
        elseif (%rencolor == %renco2-2) { set %rentxtt %rentxtt $+  $+ %renco2-2 $+ $remove($left(%rentxt,%ren),$left(%rentxt,%renn)) | set %rencolor %renco2-1 }
        goto ren2
      }
      else { 
        .say %ghablekalame_ $+  $+ %renco2-1 $+ , $+ %rencoba2 $+ %ren-txt %badekalame_
        unset %rentxt %rentxtt %renlen %ren %renn %ren-txt %§
        .halt
      }
    }
    if (%renautocolor == 3) {
      set %§ 0
      set %rencolor %renco3-2
      :rep3
      inc %§
      if ($ [ $+ [ %§ ] ]) {
        set %rentxt $ [ $+ [ %§ ] ]
        set %renlen $len(%rentxt)
        set %rentxtt  $+ %renco3-1 $upper($left($ [ $+ [ %§ ] ],1))
        set %ren 1
        set %renn 0
        :ren3
        inc %ren
        inc %renn
        if (%ren > %renlen) { set %ren-txt %ren-txt $+ %rentxtt | goto rep3 }
        if (%rencolor == %renco3-2) { set %rentxtt %rentxtt $+  $+ %renco3-2 $+ $remove($left(%rentxt,%ren),$left(%rentxt,%renn)) | set %rencolor %renco3-3 }
        elseif (%rencolor == %renco3-3) { set %rentxtt %rentxtt $+  $+ %renco3-3 $+ $remove($left(%rentxt,%ren),$left(%rentxt,%renn)) | set %rencolor %renco3-2 }
        goto ren3
      }
      else { 
        .say %ghablekalame_ $+  $+ %renco3-1 $+ , $+ %rencoba3 $+ %ren-txt %badekalame_
        unset %rentxt %rentxtt %renlen %ren %renn %ren-txt %§
        .halt
      }
    }
    if (%renautocolor == 4) {
      set %§ 0
      set %rencolor 14
      :rep4
      inc %§
      if ($ [ $+ [ %§ ] ]) {
        set %rentxt $ [ $+ [ %§ ] ]
        set %renlen $len(%rentxt)
        set %rentxtt 4 $upper( $+ $left($ [ $+ [ %§ ] ],1))
        set %ren 1
        set %renn 0
        :ren4
        inc %ren
        inc %renn
        if (%ren > %renlen) { set %ren-txt %ren-txt $+ %rentxtt | goto rep4 }
        if (%rencolor == 14) { set %rentxtt %rentxtt $+  $+ %rencolor $+ $remove($left(%rentxt,%ren),$left(%rentxt,%renn)) | set %rencolor 15 }
        elseif (%rencolor == 15) { set %rentxtt %rentxtt $+  $+ %rencolor $+ $remove($left(%rentxt,%ren),$left(%rentxt,%renn)) | set %rencolor 14 }
        goto ren4
      }
      else { 
        .say %ghablekalame_ $+ %ren-txt %badekalame_
        unset %rentxt %rentxtt %renlen %ren %renn %ren-txt %§
        .halt
      }
    }
    if (%renautocolor == 5) {
      set %§ 0
      set %rencolor 1
      :rep5
      inc %§
      if ($ [ $+ [ %§ ] ]) {
        set %rentxt $ [ $+ [ %§ ] ]
        set %renlen $len(%rentxt)
        set %rentxtt  $+ %rencolor $left($ [ $+ [ %§ ] ],1)
        bereng
        set %ren 1
        set %renn 0
        :ren5
        inc %ren
        inc %renn
        if (%ren > %renlen) { set %ren-txt %ren-txt $+ %rentxtt | goto rep5 }
        set %rentxtt %rentxtt $+  $+ %rencolor $+ $remove($left(%rentxt,%ren),$left(%rentxt,%renn))
        bereng
        goto ren5
      }
      else { 
        .say %ghablekalame_ $+  $+ %ren-txt %badekalame_
        unset %rentxt %rentxtt %renlen %ren %renn %ren-txt %§
        .halt
      }
    }
  }
  else { return }
}
menu @input-List {
  dclick:inputlist
}
menu @text_SettinG {
  dclick:rencolor
}
menu @input-List,@text_SettinG {
  Auto Color:rencolor
  -
  Fantasic Font:setfont
  -
  input Setting:inputlist
  -
}

menu @Nopm.log,@Notice.Log,@NickCall.log,@self-lag,@log,@l0g,@input-List,@text_SettinG {
  -
  Background
  .Select:/background -a $shortfn($$file="Select Photo" $mircdirPhotos\*.bmp;*.png;*.jpg)
  .None:/background -xa
  .-
  .Center:/background -ca
  .Fill:/background -fa
  .Normal:/background -na
  .Stretch:/background -ra
  .Tile:/background -ta
  .Photo:/background -pa
  -
  Clear Window:/Clear -la
  -
  Close Window:/window -c $active
  -
}

alias ren {
  clear @Text_Setting
  window -ak @Text_Setting
  if (%renautocolor == 1) {
    echo @Text_Setting %ghablekalame_ $+  $+ %renco1 $+ , $+ %rencoba1 $+ %BBB $+ $1- $+ %BBB %badekalame_
  }
  if (%renautocolor == 2) {
    set %§ 0
    set %rencolor %renco2-1
    :rep2
    inc %§
    if ($ [ $+ [ %§ ] ]) {
      set %rentxt $ [ $+ [ %§ ] ]
      set %renlen $len(%rentxt)
      set %rentxtt  $+ %rencolor $left($ [ $+ [ %§ ] ],1)
      if (%rencolor == %renco2-1) { set %rencolor %renco2-2 }
      elseif (%rencolor == %renco2-2) { set %rencolor %renco2-1 }
      set %ren 1
      set %renn 0
      :ren2
      inc %ren
      inc %renn
      if (%ren > %renlen) { set %ren-txt %ren-txt %rentxtt | goto rep2 }
      if (%rencolor == %renco2-1) { set %rentxtt %rentxtt $+  $+ %renco2-1 $+ $remove($left(%rentxt,%ren),$left(%rentxt,%renn)) | set %rencolor %renco2-2 }
      elseif (%rencolor == %renco2-2) { set %rentxtt %rentxtt $+  $+ %renco2-2 $+ $remove($left(%rentxt,%ren),$left(%rentxt,%renn)) | set %rencolor %renco2-1 }
      goto ren2
    }
    else { 
      echo @Text_Setting %ghablekalame_ $+  $+ %renco2-1 $+ , $+ %rencoba2 %ren-txt %badekalame_
      unset %rentxt %rentxtt %renlen %ren %renn %ren-txt %§
    }
  }
  if (%renautocolor == 3) {
    set %§ 0
    set %rencolor %renco3-2
    :rep3
    inc %§
    if ($ [ $+ [ %§ ] ]) {
      set %rentxt $ [ $+ [ %§ ] ]
      set %renlen $len(%rentxt)
      set %rentxtt  $+ %renco3-1 $upper($left($ [ $+ [ %§ ] ],1))
      set %ren 1
      set %renn 0
      :ren3
      inc %ren
      inc %renn
      if (%ren > %renlen) { set %ren-txt %ren-txt %rentxtt | goto rep3 }
      if (%rencolor == %renco3-2) { set %rentxtt %rentxtt $+  $+ %renco3-2 $+ $remove($left(%rentxt,%ren),$left(%rentxt,%renn)) | set %rencolor %renco3-3 }
      elseif (%rencolor == %renco3-3) { set %rentxtt %rentxtt $+  $+ %renco3-3 $+ $remove($left(%rentxt,%ren),$left(%rentxt,%renn)) | set %rencolor %renco3-2 }
      goto ren3
    }
    else { 
      echo @Text_Setting %ghablekalame_ $+  $+ %renco3-1 $+ , $+ %rencoba3 %ren-txt %badekalame_
      unset %rentxt %rentxtt %renlen %ren %renn %ren-txt %§
    }
  }
  if (%renautocolor == 4) {
    set %§ 0
    set %rencolor 14
    :rep4
    inc %§
    if ($ [ $+ [ %§ ] ]) {
      set %rentxt $ [ $+ [ %§ ] ]
      set %renlen $len(%rentxt)
      set %rentxtt 4 $upper( $+ $left($ [ $+ [ %§ ] ],1))
      set %ren 1
      set %renn 0
      :ren4
      inc %ren
      inc %renn
      if (%ren > %renlen) { set %ren-txt %ren-txt %rentxtt | goto rep4 }
      if (%rencolor == 14) { set %rentxtt %rentxtt $+  $+ %rencolor $+ $remove($left(%rentxt,%ren),$left(%rentxt,%renn)) | set %rencolor 15 }
      elseif (%rencolor == 15) { set %rentxtt %rentxtt $+  $+ %rencolor $+ $remove($left(%rentxt,%ren),$left(%rentxt,%renn)) | set %rencolor 14 }
      goto ren4
    }
    else { 
      echo @Text_Setting %ghablekalame_ $+ %ren-txt %badekalame_
      unset %rentxt %rentxtt %renlen %ren %renn %ren-txt %§
    }
  }

  if (%renautocolor == 5) {
    set %§ 0
    set %rencolor 1
    :rep5
    inc %§
    if ($ [ $+ [ %§ ] ]) {
      set %rentxt $ [ $+ [ %§ ] ]
      set %renlen $len(%rentxt)
      set %rentxtt  $+ %rencolor $left($ [ $+ [ %§ ] ],1)
      bereng
      set %ren 1
      set %renn 0
      :ren5
      inc %ren
      inc %renn
      if (%ren > %renlen) { set %ren-txt %ren-txt %rentxtt | goto rep5 }
      set %rentxtt %rentxtt $+  $+ %rencolor $+ $remove($left(%rentxt,%ren),$left(%rentxt,%renn))
      bereng
      goto ren5
    }
    else { 
      echo @Text_Setting %ghablekalame_ $+  %ren-txt %badekalame_
      unset %rentxt %rentxtt %renlen %ren %renn %ren-txt %§
    }
  }
}
alias bereng {
  if (%rencolor isnum 1-14) { inc %rencolor }
  elseif (%rencolor >= 15) { set %rencolor 1 }
  else { set %rencolor 1 }
}

;XXX PAGE XXX:
ctcp *:*:*: {
  if ($away == $true) {
    if ($1 == Page) && (%awaypage == ON) {
      window -bk @Pager
      echo @Pager 6***4 Page:2 From 10 $nick ( $+ $address $+ )
      echo @Pager 6***4 MSG:  $2-
      echo @Pager 6*** [[¯|¯]] 6***
    }
  }
}
on $*:text:$(/ $+ $me $+ /iS):#: {
  if (%highlight == ON) {
    window -n @Highlight
    echo @Highlight 7>4Attention7< 4( $+ $time(h:nn:ss TT) $+ 4) 4 Nick Call !! by7 $nick on4 $network in7 $chan with the message: $+(7",$1-,7")
    echo -a 4 Nick Call !! 3[ $+ $time $+ ]2 $nick 4Call Your Nick !!3 Nick Call !! on2 $network in4 $chan 
  }
}
on $*:action:$(/ $+ $me $+ /iS):#: {
  if (%highlight == ON) {
    window -n @Highlight
    echo @Highlight 7>4Attention7< 4( $+ $time(h:nn:ss TT) $+ 4) 4 Nick Call !! by7 $nick on4 $network in7 $chan with the message: $+(7",$1-,7")
    echo -a 4 Nick Call !! 3[ $+ $time $+ ]2 $nick 4Call Your Nick !!3 Nick Call !! on2 $network in4 $chan 
  }
}

menu @Highlight {
  Clear:clear $active
  Close:close -@ $active
}

on *:TEXT:*:#:{
  if ($me isin $1-) {
    if ($appactive == $false) {
      if ($tip(NAlert,Attention!,$nick said your name on $chan $+ .,10) == 1) { .halt }
    }
  }
}
